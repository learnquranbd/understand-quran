services:
  web:
    image: nginx:alpine
    container_name: learnquranbd_local
    ports:
      - "8080:80"
    volumes:
      - ./firebase/public:/usr/share/nginx/html:ro
      - ./local/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
